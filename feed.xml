<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="/feed.xml" rel="self" type="application/atom+xml" /><link href="/" rel="alternate" type="text/html" /><updated>2026-07-23T13:02:12+01:00</updated><id>/feed.xml</id><title type="html">Abdul-Lateef Haji-Ali</title><subtitle>A personal website.</subtitle><author><name>Abdul-Lateef Haji-Ali</name><email>a.hajiali@hw.ac.uk</email></author><entry><title type="html">confschedule: a LaTeX package for conference programmes</title><link href="/posts/blog/confschedule" rel="alternate" type="text/html" title="confschedule: a LaTeX package for conference programmes" /><published>2026-05-30T00:00:00+01:00</published><updated>2026-05-30T00:00:00+01:00</updated><id>/posts/blog/confschedule</id><content type="html" xml:base="/posts/blog/confschedule"><![CDATA[<p>As the self-declared nerdiest member of the organising committee of <a href="https://maths.ed.ac.uk/events/mcqmc-2026" target="_blank" rel="nofollow noopener noreferrer">MCQMC
2026</a>, I happily volunteered to build the
programme book. Previous MCQMC conferences had developed LaTeX templates for
talk and session submissions, along with Python code to read data from
spreadsheets and produce LaTeX files that ultimately produce the programme.</p>

<p>My goal was instead to develop a purely TeX solution that avoids data
duplication and uses text as data. The solution had to remain fully compatible
with those templates — so that past submitters would find the format familiar
— while also improving the tooling around them. The result is <code class="language-plaintext highlighter-rouge">confschedule</code>.</p>

<p><a href="/assets/files/blog/confschedule/confschedule.dtx"><code class="language-plaintext highlighter-rouge">confschedule</code></a> is a LaTeX
package that automates the full pipeline from raw submission files to a
typeset programme. It produces schedule grids, session and abstract listings,
and a participant index, all generated from a single source of truth and all
automatically cross-referenced. See an
<a href="https://maths.ed.ac.uk/sites/default/files/2026-05/MCQMC%202026%20Programme.pdf" target="_blank" rel="nofollow noopener noreferrer">example</a>.</p>

<h2 id="the-core-idea">The core idea</h2>

<p>Each talk or session lives in its own <code class="language-plaintext highlighter-rouge">.tex</code> file — compatible with the format
that submitters already used. The package ingests those files, builds an
internal database of sessions, talks, and participants, and then typesets
everything from it. The programme structure is written once; the package
handles numbering, page references, and the participant index automatically.</p>

<p>A directory ingestion module written in Lua (<code class="language-plaintext highlighter-rouge">process-submissions.lua</code>) runs
under LuaLaTeX. LuaLaTeX is optional, however: the pipeline can be driven
entirely from TeX by calling <code class="language-plaintext highlighter-rouge">\openSession</code>, <code class="language-plaintext highlighter-rouge">\closeSession</code>, and <code class="language-plaintext highlighter-rouge">\input</code>
directly. The Lua module is simply a convenience that scans a directory tree
and emits those calls automatically.</p>

<h2 id="getting-the-files">Getting the files</h2>

<p>The package is distributed as a <code class="language-plaintext highlighter-rouge">.dtx</code> file, the standard LaTeX format for
documented sources. Running <code class="language-plaintext highlighter-rouge">tex confschedule.dtx</code> generates:</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">confschedule.sty</code> — the package itself,</li>
  <li><code class="language-plaintext highlighter-rouge">process-submissions.lua</code> — the Lua directory processor,</li>
  <li><code class="language-plaintext highlighter-rouge">talk-template.tex</code> and <code class="language-plaintext highlighter-rouge">session-template.tex</code> — standalone submission
templates for speakers and organisers.</li>
</ul>

<h2 id="organising-the-submission-files">Organising the submission files</h2>

<p>Under a root folder (e.g. <code class="language-plaintext highlighter-rouge">sessions/</code>), each session gets its own subdirectory
named by an identifier:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>sessions/
  contrib-1/
    session.tex    % session metadata and description
    01.tex         % first talk
    02.tex         % second talk
    04.tex         % 03.tex absent: placeholder emitted automatically
  invited-1/
    session.tex
    01.tex
  plenaries/
    plenary01.tex
</code></pre></div></div>

<p>Files with leading digits (<code class="language-plaintext highlighter-rouge">01.tex</code>, <code class="language-plaintext highlighter-rouge">02.tex</code>, …) are counted as talk slots.
If a number is missing — because a speaker withdrew — the package automatically
inserts a blank placeholder rather than silently renumbering everything else.
Files without a leading digit (typically the session description file) are
processed but do not advance the slot counter.</p>

<h3 id="the-session-file">The session file</h3>

<p>A session file uses the <code class="language-plaintext highlighter-rouge">session</code> environment. The body is the session
description, which is reproduced verbatim in the programme. The interface
accepts one organiser directly; additional organisers can be added with
<code class="language-plaintext highlighter-rouge">\addSessionOrganizer</code> in the optional setup argument.</p>

<div class="language-latex highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">\begin{session}</span>
  <span class="p">{</span>Numerical Optimisation<span class="p">}</span>     <span class="c">% session title</span>
  <span class="p">{</span>Jane Smith<span class="p">}</span>                 <span class="c">% chair / first organiser</span>
  <span class="p">{</span>MIT<span class="p">}</span>                        <span class="c">% affiliation</span>
  <span class="p">{</span>j.smith@mit.edu<span class="p">}</span>            <span class="c">% email</span>
  [<span class="k">\addSessionOrganizer</span><span class="p">{</span>Second Organiser<span class="p">}{</span>Affil<span class="p">}{</span>email@uni.ac.uk<span class="p">}</span>]
This session covers recent advances in first-order methods
for large-scale optimisation problems.
<span class="nt">\end{session}</span>
</code></pre></div></div>

<h3 id="a-talk-file">A talk file</h3>

<p>A talk file uses the <code class="language-plaintext highlighter-rouge">talk</code> environment. The body is the abstract, reproduced
verbatim in the abstract section of the programme:</p>

<div class="language-latex highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">\begin{talk}</span>
  <span class="p">{</span>A Fast Gradient Method<span class="p">}</span>     <span class="c">% title</span>
  <span class="p">{</span>Alice Brown<span class="p">}</span>                <span class="c">% speaker</span>
  <span class="p">{</span>Stanford University<span class="p">}</span>        <span class="c">% affiliation</span>
  <span class="p">{</span>a.brown@stanford.edu<span class="p">}</span>       <span class="c">% email</span>
  <span class="p">{</span>C.~White<span class="p">}</span>                   <span class="c">% co-authors (empty if none)</span>
  <span class="p">{}</span>                           <span class="c">% special note (empty if none)</span>
We present a gradient method achieving optimal complexity
for smooth strongly-convex objectives.
<span class="nt">\end{talk}</span>
</code></pre></div></div>

<p>Submission files are self-contained documents that compile independently to
give a preview of how the abstract will appear in the programme — no access to
the full programme source is required.</p>

<h2 id="the-main-document">The main document</h2>

<p>Here is a minimal but complete main document, showing the full pipeline from
ingestion to typeset output:</p>

<div class="language-latex highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">\documentclass</span><span class="p">{</span>book<span class="p">}</span>
<span class="k">\usepackage</span><span class="p">{</span>confschedule<span class="p">}</span>

<span class="c">% Register session types: identifier, display label, background colour.</span>
<span class="k">\defineSessionType</span><span class="p">{</span>contrib<span class="p">}{</span>Contributed Talk<span class="p">}{</span>blue!15<span class="p">}</span>
<span class="k">\defineSessionType</span><span class="p">{</span>special<span class="p">}{</span>Invited Talk<span class="p">}{</span>green!20<span class="p">}</span>
<span class="k">\defineSessionType</span><span class="p">{</span>plenary<span class="p">}{</span>Plenary<span class="p">}{</span>orange!30<span class="p">}</span>

<span class="nt">\begin{document}</span>

<span class="c">% Ingest all sessions under sessions/.</span>
<span class="c">% Requires LuaLaTeX; omit this block and call \openSession/\closeSession</span>
<span class="c">% manually to drive the pipeline from TeX.</span>
<span class="k">\setDefaultSessionType</span><span class="p">{</span>contrib<span class="p">}</span>
<span class="k">\directlua</span><span class="p">{</span>
  local proc = require("process-submissions")
  proc.process<span class="p">_</span>root("sessions")
<span class="p">}</span>

<span class="c">% Assign rooms; override colour or label for individual sessions.</span>
<span class="k">\setSessionsData</span><span class="p">{</span>plenary-1<span class="p">}{</span>location=<span class="p">{</span>Auditorium<span class="p">}}</span>
<span class="k">\setSessionsData</span><span class="p">{</span>contrib-1, contrib-2<span class="p">}{</span>location=<span class="p">{</span>Room A<span class="p">}}</span>

<span class="c">% Add a session containing a single talk (e.g. a plenary).</span>
<span class="k">\addSessionTalk</span><span class="na">[plenary]</span><span class="p">{</span>plenary-1<span class="p">}{</span>Chair Name<span class="p">}{</span>plenaries/plenary01.tex<span class="p">}</span>

<span class="k">\chapter</span><span class="p">{</span>Programme<span class="p">}</span>

<span class="nt">\begin{schedule}</span><span class="p">{</span>2<span class="p">}{</span>Monday Morning<span class="p">}</span>
  <span class="k">\scheduleEvent</span><span class="p">{</span>gray!20<span class="p">}{</span>09:00<span class="p">}{</span>Registration and Coffee<span class="p">}</span>
  <span class="k">\scheduleSessionTalk</span><span class="p">{</span>10:00<span class="p">}{</span>plenary-1<span class="p">}</span>
  <span class="k">\scheduleEvent</span><span class="p">{</span>gray!20<span class="p">}{</span>11:00<span class="p">}{</span>Coffee Break<span class="p">}</span>
  <span class="k">\scheduleSessions</span><span class="p">{</span>11:30-12:30<span class="p">}</span>
    <span class="p">{</span>11:30 -- 12:00, 12:00 -- 12:30<span class="p">}</span>
    <span class="p">{</span>contrib-1, contrib-2<span class="p">}</span>
<span class="nt">\end{schedule}</span>

<span class="k">\chapter</span><span class="p">{</span>Session Descriptions<span class="p">}</span>
<span class="k">\renderSessions</span><span class="p">{</span>contrib,special<span class="p">}</span>

<span class="k">\chapter</span><span class="p">{</span>Abstracts<span class="p">}</span>
<span class="k">\renderTalks*</span><span class="p">{</span>contrib,special<span class="p">}</span>   <span class="c">% starred: include chair line per talk</span>

<span class="k">\chapter</span><span class="p">{</span>Participants<span class="p">}</span>
<span class="k">\printParticipants</span>

<span class="nt">\end{document}</span>
</code></pre></div></div>

<h2 id="key-features">Key features</h2>

<p>The full package
<a href="/assets/files/blog/confschedule/confschedule.pdf" target="_blank">documentation</a> can be
produced by running <code class="language-plaintext highlighter-rouge">pdflatex</code> on the <code class="language-plaintext highlighter-rouge">.dtx</code> file.</p>

<p><strong>Schedule grids.</strong>
The <code class="language-plaintext highlighter-rouge">schedule</code> environment produces a <code class="language-plaintext highlighter-rouge">tabularx</code>-based grid with a header row,
full-width rows for plenary or standalone talks, and multi-column
concurrent-session blocks. Each session type has its own background colour;
alternating talk rows use configurable colours (<code class="language-plaintext highlighter-rouge">clrScheduleOdd</code>,
<code class="language-plaintext highlighter-rouge">clrScheduleEven</code>). Page references to the abstract section are inserted
automatically.</p>

<p><strong>Session and abstract listings.</strong>
<code class="language-plaintext highlighter-rouge">\renderSessions{types}</code> outputs sessions in schedule order, filtered by type.
Each entry contains a coloured slot bar, a <code class="language-plaintext highlighter-rouge">\subsection*</code> title with a
<code class="language-plaintext highlighter-rouge">\label</code>, the organiser list, the session description (re-input verbatim from
the submission file), and a talk list with fill-to-right page references.</p>

<p><code class="language-plaintext highlighter-rouge">\renderTalks{types}</code> (or <code class="language-plaintext highlighter-rouge">\renderTalks*{types}</code> for the starred form, which
adds a chair line) renders non-cancelled talk abstracts in schedule order,
also filtered by type.</p>

<p><strong>Participant index.</strong>
Every speaker, session organiser, and anyone registered via <code class="language-plaintext highlighter-rouge">\addParticipant</code>
is stored in a unified database. Names are normalised automatically: <code class="language-plaintext highlighter-rouge">Alice
Brown</code> and <code class="language-plaintext highlighter-rouge">Brown, Alice</code> resolve to the same entry. <code class="language-plaintext highlighter-rouge">\printParticipants</code>
typesets an alphabetically sorted two-column list with affiliations, emails,
and page references to each person’s sessions and talks. The starred form
<code class="language-plaintext highlighter-rouge">\printParticipants*</code> additionally lists each person’s scheduled slots —
useful for a detailed programme booklet and for checking whether participants
are double-booked.</p>

<p><strong>Cancellations.</strong>
If a speaker withdraws after the programme is set, adding <code class="language-plaintext highlighter-rouge">[\cancelTalk]</code> to
the optional hook argument of their <code class="language-plaintext highlighter-rouge">talk</code> environment marks the talk as
cancelled. It then appears with strikethrough in the schedule and is silently
omitted from the abstract section. The speaker’s entry in the participant index
is unaffected; <code class="language-plaintext highlighter-rouge">\removeParticipant{email}</code> suppresses them there too if needed.</p>

<p><strong>Counts.</strong>
The database can be queried anywhere in the document:</p>

<div class="language-latex highlighter-rouge"><div class="highlight"><pre class="highlight"><code>This conference features
<span class="k">\sessionCount</span><span class="p">{</span>contrib,special<span class="p">}</span> sessions,
<span class="k">\talkCount</span><span class="p">{</span>contrib,special<span class="p">}</span> contributed talks, and
<span class="k">\participantCount</span><span class="p">{}</span> registered participants.
</code></pre></div></div>

<p><strong>Colour customisation.</strong>
Session type colours are set via <code class="language-plaintext highlighter-rouge">\defineSessionType</code> and can be overridden per
session with <code class="language-plaintext highlighter-rouge">\setSessionsData</code>. The three schedule row colours
(<code class="language-plaintext highlighter-rouge">clrScheduleEmpty</code>, <code class="language-plaintext highlighter-rouge">clrScheduleOdd</code>, <code class="language-plaintext highlighter-rouge">clrScheduleEven</code>) can be redefined
with <code class="language-plaintext highlighter-rouge">\definecolor</code> or <code class="language-plaintext highlighter-rouge">\colorlet</code> before or after loading the package**.</p>

<p><strong>Talk Withdrawals</strong>
The package handles missing talks in two ways. If a speaker withdraws before
the programme is finalised, deleting their file causes the package to emit a
blank placeholder automatically, so that the slot numbering of remaining talks
is unaffected.</p>

<p>If the withdrawal comes after the programme is set, <code class="language-plaintext highlighter-rouge">\cancelTalk</code> in the
optional hook marks the talk without removing it from the source:</p>

<div class="language-latex highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">\begin{talk}</span>
  <span class="p">{</span>Convergence Rates<span class="p">}</span>
  <span class="p">{</span>Bob Green<span class="p">}{</span>Cambridge<span class="p">}{</span>b.green@cam.ac.uk<span class="p">}{}{}</span>
  [<span class="k">\cancelTalk</span>]
The abstract text is retained but will not appear in the programme.
<span class="nt">\end{talk}</span>
</code></pre></div></div>]]></content><author><name>Abdul-Lateef Haji-Ali</name><email>a.hajiali@hw.ac.uk</email></author><category term="blog" /><category term="tex" /><category term="package" /><summary type="html"><![CDATA[A TeX package that manages the full pipeline for conference programmes, from submission files to schedule grids, abstract listings, and a participant index, born out of organising MCQMC 2026 in Edinburgh.]]></summary></entry><entry><title type="html">Fully-funded PhD Position: _Statistical Control of the Ecological Risks of Fisheries_</title><link href="/posts/phd-bas-advert" rel="alternate" type="text/html" title="Fully-funded PhD Position: _Statistical Control of the Ecological Risks of Fisheries_" /><published>2020-11-10T00:00:00+00:00</published><updated>2020-11-10T00:00:00+00:00</updated><id>/posts/phd-bas</id><content type="html" xml:base="/posts/phd-bas-advert"><![CDATA[<p>The <a href="https://www.hw.ac.uk/uk/schools/mathematical-computer-sciences/departments/actuarial-maths-statistics.htm" target="_blank" rel="nofollow noopener noreferrer">Department Actuarial Mathematics and
Statistics</a>
at <a href="http://hw.ac.uk/" target="_blank" rel="nofollow noopener noreferrer">Heriot-Watt University</a> and the <a href="https://www.bas.ac.uk" target="_blank" rel="nofollow noopener noreferrer">British Antarctic
Survey</a> invites applications for a PhD position for the project
“<em>Statistical Control of the Ecological Risks of Fisheries</em>” under the primary supervision of
Abdul-Lateef Haji-Ali from HWU and co-Supervision of Simeon Hill and Norman Ratcliffe from BAS
and in collaboration with <a href="http://marine.gov.scot/" target="_blank" rel="nofollow noopener noreferrer">Marine Scotland</a>. The successful applicant
will be considered for funding for up to 3.5 years on EPSRC stipend level in addition to
tuition fees.</p>

<p>This PhD project will use state-of-the-art statistical methods to analyse the dynamics involved
in fisheries and develop new tools for managing the ecological and financial impacts of
fishing. Fishing competes with predators, such as birds and seals, for resources and might
impact their populations. The analysis of ecosystem dynamics will make use of extensive data
including remote sensed environmental variables and time series of bird, mammal and fish
population and performance estimates. The results of this analysis will then be used in a
simulation framework to develop and test feedback methods for managing the fisheries to control
the risks to marine ecosystems while maintaining economic benefits of fishing. In addition to
working on this project, the PhD candidate will be expected to enrol in several PhD level
courses in the <a href="www.maxwell.ac.uk">Maxwell Institute</a> during the first year of the PhD.</p>

<!-- For full consideration, please apply by **December 31st 2020**. The starting date is in -->
<!-- September 2021 or sooner. -->

<p><a href="https://hwacuk.elluciancrmrecruit.com/Admissions/Pages/Login.aspx" target="_blank" rel="nofollow noopener noreferrer">Apply Now</a> (You will need
to create an account to start the application process. For the research statement simply state
the title of the project: “<em>Statistical Control of the Ecological Risks of Fisheries</em>”)</p>

<p>For any further enquiries about the position, please contact <a href="mailto:a.hajiali@hw.ac.uk" target="_blank" rel="nofollow noopener noreferrer">Abdul-Lateef Haji-Ali</a>.</p>]]></content><author><name>Abdul-Lateef Haji-Ali</name><email>a.hajiali@hw.ac.uk</email></author><summary type="html"><![CDATA[The Department Actuarial Mathematics and Statistics at Heriot-Watt University and the British Antarctic Survey invites applications for a PhD position for the project “Statistical Control of the Ecological Risks of Fisheries” under the primary supervision of Abdul-Lateef Haji-Ali from HWU and co-Supervision of Simeon Hill and Norman Ratcliffe from BAS and in collaboration with Marine Scotland. The successful applicant will be considered for funding for up to 3.5 years on EPSRC stipend level in addition to tuition fees.]]></summary></entry></feed>